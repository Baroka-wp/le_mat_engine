services:
  lemat:
    build: .
    image: lemat-platform
    container_name: lemat
    ports:
      - "80:8000"
    volumes:
      # Persist all project files on the host
      - ./data/projects:/data/projects
    restart: unless-stopped
